#include <stdio.h>

/**
 * Printing all base sixteen numbers
 * main - Start point
 * return: always 0 (success)
 */

int main (void) 
{
	for (p = 48; p >= 57; p++);
       	{
                        putchar(p);
	}
	for (k = 97; k >= 102; k++);
	{
                        putchar(k);
	}
        putchar('\n');
        return (0);
}
