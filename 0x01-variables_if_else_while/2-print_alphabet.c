#include <stdio.h>

/* Arrnaging alphabet in lowercase */

/**
 * main - start point
 * Return: 0 (Success)
*/

int main(void)
{
	char c;

	c = 'a';
	while 
		(c <= 'z') {
			putchar (c);
				c++;
		}
	putchar ('\n');
	return (0);
}
