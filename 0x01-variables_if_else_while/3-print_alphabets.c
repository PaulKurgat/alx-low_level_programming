#include <stdio.h>

/* Arranging alphabet in lowercas then upper */

/**
 * main - start point
 * Return: 0 (Success)
*/

int main(void)
{
        char c;

	char x;

        c = 'a';

	c = 'A';
	while
                (c <= 'z') {
                        putchar (c);
                        c++;
                }
	while
                (c <= 'Z') {
                        putchar (c);
                        c++;
		}
        putchar ('\n');
        return (0);
}
