#include "main.h"
#include <stdio.h>

/**
 * mul - finds the multiples of integers
 * @a: - the first input 
 * @b: - the second input
 *
 * Return: Always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
