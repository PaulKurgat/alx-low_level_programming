#include "main.h"

/**
 * This fucntion checks lower case
 * c: The input to be checked
 * result: 1 for capital and 0 for lower case
 */

int _isupper(int c)
{
if (c >=65 && c <= 90)
return (1);
else
return (0);
}
